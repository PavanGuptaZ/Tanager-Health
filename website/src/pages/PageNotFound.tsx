
export const PageNotFound = ({ type }: { type: string }) => {
    return (
        <div style={{
            minHeight: "calc(100vh - 241px)",
            background: "#F3F4F8",
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            flexDirection: "column",
            fontSize: "3rem"
        }}>PageNotFound</div>
    )
}
